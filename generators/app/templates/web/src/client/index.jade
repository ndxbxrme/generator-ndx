doctype strict
html
  head
    base(href='/')
    meta(charset='utf-8')
    title <%= settings.appName %>
    meta(name='description', content='<%= settings.description %>')
    meta(name="viewport" content="user-scalable=no, width=device-width, initial-scale=1, maximum-scale=1")
  body(ng-app='<%= settings.appName %>')<% if(settings.Header) { %>
    header<% } %><% if(settings.Menu) { %>
    menu.mobile-menu(ng-show='mobileMenuOut')<% } %><% if(settings.Local) { %>
    login<% } %>
    .body
      .view(ui-view='')<% if(settings.Footer) { %>
    footer<% } %>